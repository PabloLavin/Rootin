package com.herlavroc.proyectofinal

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Toast
import com.google.firebase.FirebaseApp
import org.json.JSONArray
import org.json.JSONObject
import com.google.firebase.firestore.FirebaseFirestore
import com.herlavroc.proyectofinal.model.Usuario


class MainActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
       /*fun usuRutinas(view: View)
        {
            val Rutinas= Intent(this,
                usuario_rutinas::class.java)
            startActivity(Rutinas)
        }*/

        /*
           lateinit var imagen: String
    lateinit var nombre: String
    lateinit var apellido: String
    lateinit var correo: String
    lateinit var contrasenia: String
    lateinit var telefono: String
         */


    }
}
